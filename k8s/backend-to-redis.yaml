apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  namespace: lincode
  name: backend-to-redis
  namespace: lincode

spec:
  podSelector:
    matchLabels:
      app: redis

  policyTypes:
    - Ingress

  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: backend
      ports:
        - protocol: TCP
          port: 6379
