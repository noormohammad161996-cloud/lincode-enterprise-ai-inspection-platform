apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: backend-to-ai
  namespace: default

spec:
  podSelector:
    matchLabels:
      app: ai

  policyTypes:
    - Ingress

  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: backend
      ports:
        - protocol: TCP
          port: 8001
