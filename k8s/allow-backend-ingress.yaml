apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  namespace: lincode
  name: allow-backend-ingress
  namespace: lincode

spec:
  podSelector:
    matchLabels:
      app: backend

  policyTypes:
    - Ingress

  ingress:
    - ports:
        - protocol: TCP
          port: 8000
