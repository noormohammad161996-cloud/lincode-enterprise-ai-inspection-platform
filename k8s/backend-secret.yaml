apiVersion: v1

kind: Secret

metadata:
  namespace: lincode
  name: backend-secret

type: Opaque

stringData:

  DATABASE_URL: "postgresql://postgres:postgres@postgres-service:5432/lincode_db"

  SECRET_KEY: "change-this-secret-key"
